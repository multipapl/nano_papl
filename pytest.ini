[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
norecursedirs = tests/tests_ref
addopts = -v --cov=core --cov=ui --cov-report=term-missing
# Add current directory to python path
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning:google.genai.*
